from typing import List


class Solution:
    def findPeakGrid(self, mat: List[List[int]]) -> List[int]:
        c_c = len(mat)//2
        side = None
        found = False
        while not found:
            
        return [0, 0]
